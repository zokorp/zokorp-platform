// Test stub for Next.js's `server-only` marker package, which is not resolvable in the vitest (node)
// environment. Importing it is a no-op here; the real package only guards against client bundling.
export {};
